[package]
name = "mosaico-core"
version = "0.3.1"
edition = "2024"
description = "Platform-agnostic core types and traits for Mosaico"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"

[lints]
workspace = true
