[package]
name = "mosaico-core"
version = "0.1.0"
edition = "2024"
description = "Platform-agnostic core types and traits for Mosaico"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lints]
workspace = true
